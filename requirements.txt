# ── cœur de l’app ───────────────────────────────────────────────
streamlit==1.44.1
firebase-admin==6.5.0
pandas>=2.2,<3.0
numpy>=1.26
altair>=5.2
pytz>=2024.1

# ── auth Google ────────────────────────────────────────────────
google-auth>=2.28
google-auth-httplib2>=0.2.0
google-api-python-client>=2.126

# ── visualisation & UI avancée ─────────────────────────────────
streamlit-echarts>=0.4.0      # donuts / gauges / graphes interactifs
plotly>=5.19                  # (optionnel) line & bar charts animés
streamlit-extras>=0.3.0       # (optionnel) icônes Material, badges, etc.

# ── table interactive si tu en as besoin ───────────────────────
streamlit-aggrid==0.3.4
